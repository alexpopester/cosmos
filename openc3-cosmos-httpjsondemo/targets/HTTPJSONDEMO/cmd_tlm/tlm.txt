TELEMETRY HTTPJSONDEMO INBOUND BIG_ENDIAN "Inbound JSON POST from an external source via HttpJsonServerInterface."
  # POST to /httpjsondemo/inbound — item names must match incoming JSON keys exactly.
  # Keyword   Name         BitSize  Type    Description
  APPEND_ITEM temperature  64       FLOAT   "Temperature reading"
  APPEND_ITEM pressure     64       FLOAT   "Pressure reading"
  APPEND_ITEM label        0        STRING  "Sensor identifier label"

TELEMETRY HTTPJSONDEMO RESPONSE BIG_ENDIAN "Successful API response (2xx). JSON keys map to fields by name."
  # HTTP_STATUS is injected automatically into packet.extra — no declaration needed.
  # Keyword   Name         BitSize  Type    Description
  APPEND_ITEM status       0        STRING  "Response status string from the API"
  APPEND_ITEM message      0        STRING  "Human-readable message from the API"
  APPEND_ITEM temperature  64       FLOAT   "Echoed temperature value"
  APPEND_ITEM pressure     64       FLOAT   "Echoed pressure value"

TELEMETRY HTTPJSONDEMO ERROR BIG_ENDIAN "Error API response (3xx/4xx/5xx). Routed here when ERROR_PACKET is configured."
  # HTTP_STATUS is injected automatically into packet.extra — no declaration needed.
  # Keyword   Name     BitSize  Type    Description
  APPEND_ITEM error    0        STRING  "Error type or title from the API"
  APPEND_ITEM detail   0        STRING  "Detailed error description"
  APPEND_ITEM code     32       UINT    "Application-level error code"
